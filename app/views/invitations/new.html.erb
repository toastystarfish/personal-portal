<div class="form-page">
  <%= form_for(@invitation) do |f| %>
  <% if @invitation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invitation.errors.count, "error") %> prohibited this invitation from being saved:</h2>

      <ul>
        <% @invitation.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :invited_by_id  %>
  <h1>Invite New User</h1>
  <div class="field">
    <%= f.label :email %>
    <%= f.email_field :email, :required => true %>
  </div>
  <div class="actions">
    <%= f.submit "Send Invitation", :class => "button submit" %>
    <%= link_to "Cancel", users_path, :class => "cancel button" %>
  </div>
  <% end %>
</div>
